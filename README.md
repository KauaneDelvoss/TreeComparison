# TreeComparison
That's a paper comparing the algorithm complexity between the AVL, B and Red-black trees.

To run the project, it is necessary to install python, pandas and matplotlib.

Open the terminal and run python main.py in the root of the project.
